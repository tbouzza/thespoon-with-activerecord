<h1>Restaurants</h1>

<ul>
  <% @restaurants.each do |restaurant| %>
    <li>
      <h3><%= link_to restaurant.name, restaurant_path(restaurant) %> 🦄 * <%= restaurant.rating %></h3>
    </li>
  <% end %>
</ul>
